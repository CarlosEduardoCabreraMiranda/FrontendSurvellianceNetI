export class Cliente {
    id!: number;
    nombre!: string;
    apellido!: string;
    createAt!: string;
    email!: string;
}//El signo ! que se pone en la declaración de una variable es para inicializar implícitamente esas variables dentro de la clase.
